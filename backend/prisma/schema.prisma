generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Mock {
  id          Int    @id @default(autoincrement())
  name        String
  url         String @unique
  status_code String
  body        String
}
